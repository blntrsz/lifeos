[tasks.dev]
run = "bunx --bun vite dev --host 0.0.0.0 --port 3000"

[tasks.build]
run = "bunx --bun vite build"

[tasks.preview]
run = "bunx --bun vite preview --host 0.0.0.0 --port 3000"

[tasks.test]
run = "bunx vitest run"
