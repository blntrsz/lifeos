# External packages

You can explore external packages using ./.gitmodules ; (e.g. effect package in effect-smol)

# Use mise

For running tasks we use mise (https://mise.jdx.dev/)

## Agent skills

### Issue tracker

Issues and PRDs are tracked in GitHub Issues for `blntrsz/lifeos`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the canonical triage labels: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context layout: read root `CONTEXT-MAP.md` to find relevant per-context `CONTEXT.md` files and ADRs. See `docs/agents/domain.md`.
